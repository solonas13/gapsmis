Example 1.
----------

After compiling GapsMis, you can run an example for global alignment via

$ ./gapsmis -a ./data/seqa.fa -b ./data/seqb.fa

You can see the generated output via

$ cat gapsmis.out


Example 2.
----------

You can run an example for local alignment via

$ ./gapsmis -a ./data/lseqa.fa -b ./data/lseqb.fa -L 1 -l 3

You can see the generated output via

$ cat gapsmis.out


Example 3.
----------

Change to directory `exp' and follow the instructions given
in file README.
